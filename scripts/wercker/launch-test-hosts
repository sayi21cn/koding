#!/bin/bash

SCRIPTS_DIR=$WERCKER_ROOT/scripts

set -o errexit

BRANCH=${BRANCH:-$WERCKER_GIT_BRANCH}
INSTANCE_OPTIONS=$($SCRIPTS_DIR/test-instance/get-options)
INSTANCE_OPTIONS="--branch-name $BRANCH $INSTANCE_OPTIONS"
echo $INSTANCE_OPTIONS

$SCRIPTS_DIR/test-instance/launch $INSTANCE_OPTIONS > INSTANCE_DATA
cat INSTANCE_DATA

exit 0
